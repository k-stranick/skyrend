/*
package dtcc.itn262.items;

import dtcc.itn262.character.Player;

public class HealthStim extends Items {
	private int healthRestored = 40;

	public HealthStim(String name, int value, int weight, int healthRestored) {
		super(name, value, weight);
		this.healthRestored = healthRestored;
	}

	public int getHealthRestored() {
		return healthRestored;
	}
public void useItem(Player player) {
		player.getPlayerAttributes().setHealth(player.getPlayerAttributes().getHealth() + healthRestored);
		System.out.println(player.getHero() + " uses " + getName() + " and restores " + healthRestored + " health.");
	}
}
*/
